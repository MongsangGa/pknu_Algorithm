#include <bits/stdc++.h>

using namespace std;

#define fastio cin.tie(0), ios_base::sync_with_stdio(0)

#define SIZE 10'003

int k, n, max;
int arr[SIZE];

int main(){

    fastio;
    cin >> k >> n; // k개의 랜선 n개의 랜선 만드는 최대길이는?

    for (int i=0; i<k; i++)
        cin >> arr[i];


    return 0;
}
